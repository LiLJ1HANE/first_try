name: Build and Publish image to Docker Hub
on: 
    [workflow_dispatch]
jobs:
  publish_images:
runs-on: ubuntu-latest
steps:
- name: Checkout code
  uses: actions/checkout@v4
-name: build image
run: docker build ./api/ -t tah a7486/backend:latest
-name: push image to docker hub
run: |
docker push taha7486/backend:latest

